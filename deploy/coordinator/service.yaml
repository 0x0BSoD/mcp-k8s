apiVersion: v1
kind: Service
metadata:
  name: mcp-coordinator
  namespace: mcp-system
  labels:
    app: mcp-coordinator
spec:
  type: ClusterIP
  selector:
    app: mcp-coordinator
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
